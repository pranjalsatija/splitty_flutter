name: splitty
description: Splitty, rewritten in Flutter.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_mobx: 0.0.14
  intl: 0.15.7
  path: 1.6.2
  path_provider: 0.4.1

flutter:
  uses-material-design: true
  assets:
  - assets/strings.json